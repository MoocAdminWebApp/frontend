export enum Gender {
  Other = 0,
  Male = 1,
  Female = 2,
}

export enum MenuType {
  Dir = 1,
  Menu = 2,
  Btn = 3,
}

export enum EAccessType {
  Admin = 1,
  Teacher = 2,
  Student = 3,
}

export enum StatusType {
  Active = 1,
  Inactive = 2,
  Draft = 3,
  Deleted = 4,
}
